#include "geometry/Shape.hpp"

Shape::Shape(unsigned int att_count ,unsigned int vertex_count, unsigned int indices_count):vertex_count(vertex_count),indices_count(indices_count),att_count(att_count){
    //Do nothing
}